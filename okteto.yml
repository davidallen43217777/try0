build:
  att:
    image: okteto.dev/node:0.0.5
    context: .
    args:
      PORT: ${PORT}
      TRYVAROJ: ${PORT}
deploy:
  - kubectl apply -f okteto-k8s.yml
dev:
  att:
    command: bash
    sync:
      - .:/usr/src/app
    forward:
      - 9229:9229
dependencies:
  att:
    #repository: https://github.com/okteto/movies-frontend
    #manifest: okteto.yml
    #branch: main
    variables:
      OJABESA: THEBEST
    #wait: true